using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandEncounterSpawn : MonoBehaviour
{
    /* determine a vector3 based off random x and z positions (y stays constant) 
     * and instantiate new encounter points based off it */ 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
